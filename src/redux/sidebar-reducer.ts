let initialState = {

}

type InitialStateType = typeof initialState;

const sidebarReduccer = (state = initialState, action:any):InitialStateType => {
    return state;
}

export default sidebarReduccer;